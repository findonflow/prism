/*--------------------------------------------------------------------------------------------------------------------*/
import { z } from "zod";

/*--------------------------------------------------------------------------------------------------------------------*/
export function zFlowAddress (label: string){
  return z.string()
    .min(1, `${label} address is required`)
    .regex(/^0x[a-fA-F0-9]{16}$/, `Invalid ${label} address`);
}